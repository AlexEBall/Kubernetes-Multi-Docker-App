apiVersion: apps/v1
kind: Deployment
metadata:
  name: client-deployment
spec:
  replicas: 3
  selector:
    matchLabels:
      component: web
  template:
    metadate:
      labels:
        component: web
    spec:
      containers:
        - name: client
          image: alexeball/fib-calc-client
          ports:
            - containerPort: 3000